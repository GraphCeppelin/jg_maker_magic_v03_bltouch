# VERIFY FLASH REPORT — Byte-by-Byte Comparison

**Дата**: 2026-09-18
**Метод**: `cmp` (WSL, avr-objcopy ihex→binary)
**Файлы**: `FLASH_backup_2026-09-18.hex` → `backup.bin` (261 426 B) vs `flash_verify_full.bin` → `current.bin` (261 426 B)

---

## Результат: ✅ ПОДТВЕРЖДЕНО

---

## 1. Общая статистика

| Параметр | Значение |
|----------|----------|
| Размер backup.bin | 261 426 байт |
| Размер current.bin | 261 426 байт |
| **Всего различающихся байт** | **172 978** |
| Первое различие | `0x00003` (4-й байт) |
| Последнее различие | `0x2B436` (конец app + 1) |
| Область идентичных байт | `0x2B436` – конец файла (gap + bootloader) |

---

## 2. Пообластное сравнение

| Область | Адрес | Размер | Статус |
|---------|-------|--------|--------|
| **App** (Marlin) | `0x00000 – 0x2B435` | 177 206 B | **РАЗЛИЧАЕТСЯ** — 172 978 B (97.61%) — **ожидаемо**: новый firmware |
| **Gap** (free) | `0x2B436 – 0x3DFFF` | 79 866 B | **ИДЕНТИЧЕН** ✅ |
| **Bootloader** | `0x3E000 – 0x3FD31` | 7 474 B | **✅✅✅ ИДЕНТИЧЕН** — **НЕ ТРОНУТ** |

---

## 3. Bootloader — побайтово идентичен

Оба файла содержат **одинаковые** строки:
```
ATmega2560
Arduino explorer stk500V2 by MLS
Bootloader>
Huh?
Compiled on = 
CPU Type    = 
__AVR_ARCH__= 
AVR LibC Ver= 
GCC Version = 
CPU ID      = 
```

`cmp` подтверждает: **0 отличий в 7 474 байтах bootloader'а.**

---

## 4. Gap — идентичен

79 866 байт между app и bootloader — **побайтово совпадают**. ~97% этого региона — `0xFF` (пустая флеша).

---

## 5. App — различается (ожидаемо)

- 177 206 байт в app-регионе
- 172 978 байт **различаются** (97.61%)
- 4 228 байт **совпадают** (2.39% — типично для одинаковых векторов прерываний, констант)
- Различия **кончаются ровно на `0x2B436`** — не выходят за пределы app-региона

---

## 6. Вывод

| Вопрос | Ответ |
|--------|-------|
| Записан ли новый firmware? | ✅ Да — app-регион различается (97.61% новых байт) |
| Тронут ли bootloader? | ❌ **Нет** — 7 474 байта идентичны backup |
| Тронут ли gap? | ❌ **Нет** — идентичен |
| Вышли ли записи за пределы app? | ❌ **Нет** — последнее различие = `0x2B436` (граница app) |
| Прошивка успешна? | ✅ **ДА** |
